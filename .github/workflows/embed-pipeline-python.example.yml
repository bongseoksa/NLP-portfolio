# GitHub Actions Python 임베딩 파이프라인 예시
#
# 이 파일은 설계 문서의 예시 workflow입니다.
# 실제 사용 시에는 .github/workflows/embed-pipeline.yml로 복사하여 사용하세요.
#
# 자세한 내용은 docs/architecture/GITHUB_ACTIONS_PYTHON_PIPELINE.md 참고

# name: Embedding Pipeline (Python)
# 
# on:
#   schedule:
#     - cron: "0 18 * * 0"
#   workflow_dispatch:
#     inputs:
#       reset:
#         description: "Reset ChromaDB collection"
#         required: false
#         type: boolean
#         default: false
# 
# # ... (나머지 내용은 GITHUB_ACTIONS_PYTHON_PIPELINE.md 참고)

